console.log('hi');

const newArr = [1, 2, 3, 4];

// const squaredArr = newArr.map((el) => el ** 2);

// const evensArr = newArr.filter((el) => el % 2 === 0);

const findArr = newArr.find((el) => el % 2 === 0);

// const evensArr = squaredArr.filter((el) => el % 2);

console.log(findArr);
